package com.ivi.app.food.model;

public enum FoodSource {

    /** Approximate development data. Not measured composition, not for clinical use. */
    DEMO_SEED,

    /** Created or overridden by a practitioner. */
    PRACTITIONER
}
